import { mount } from 'vue-test-utils';
import App from '../components/App.vue';

describe('App (root) component', () => {

    describe('Lifecycle tests', () => {

        it("App should render HTML containing a div with router-view", () => {
            expect("Goeie").toBe("Goeie");
        });




    });


});
